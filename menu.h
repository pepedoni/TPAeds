#include <stdio.h>

#ifndef MENU_H_INCLUDED
#define MENU_H_INCLUDED

// Exibe o menu em que o usuario seleciona o nivel de jogo
void menu(int* numDigitos, int* nivel, int* maxNumber, char* repete);

#endif // MENU_H_INCLUDED
